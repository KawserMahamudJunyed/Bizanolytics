"use client"

import { useState, useEffect } from "react"

import { motion } from "framer-motion"
import { ArrowUpRight, AlertTriangle, CheckCircle, Clock, Sparkles } from "lucide-react"
import { cn } from "@/lib/utils"

// Removed static forecastItems

const statusConfig = {
  "low-stock": {
    icon: AlertTriangle,
    color: "text-amber-500",
    bg: "bg-amber-500/10",
    border: "border-amber-500/20",
    label: "Low Stock",
  },
  critical: {
    icon: AlertTriangle,
    color: "text-red-500",
    bg: "bg-red-500/10",
    border: "border-red-500/20",
    label: "Critical",
  },
  optimal: {
    icon: CheckCircle,
    color: "text-emerald-500",
    bg: "bg-emerald-500/10",
    border: "border-emerald-500/20",
    label: "Optimal",
  },
}

// Removed static ForecastTable

// Removed static insights

import { useData } from "@/contexts/DataContext"
import { useLanguage } from "@/contexts/LanguageContext"

export function AIInsights() {
  const { rawData, aiInsights: cachedInsights, saveAiInsights } = useData()
  const { language, t } = useLanguage()
  const [aiInsight, setAiInsight] = useState<string | null>(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    async function fetchInsight() {
      if (!rawData || rawData.length === 0) {
        setAiInsight("Please upload data to generate insights.")
        setLoading(false)
        return
      }
      
      if (cachedInsights) {
        try {
          const parsed = JSON.parse(cachedInsights)
          if (parsed[language]) {
            setAiInsight(parsed[language])
            setLoading(false)
            return
          }
        } catch(e) {
          // Legacy string fallback
          if (language === 'en') {
            setAiInsight(cachedInsights)
            setLoading(false)
            return
          }
        }
      }

      setLoading(true)
      try {
        const res = await fetch("/api/forecast-insight", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ rawData, language })
        })
        const data = await res.json()
        setAiInsight(data.insight)
        
        // Merge into JSON
        let newInsights: Record<string, string> = {}
        if (cachedInsights) {
          try {
            newInsights = JSON.parse(cachedInsights)
          } catch(e) {
            newInsights = { en: cachedInsights }
          }
        }
        newInsights[language] = data.insight
        saveAiInsights(JSON.stringify(newInsights))
      } catch (e) {
        setAiInsight("Error fetching AI insight.")
      } finally {
        setLoading(false)
      }
    }
    fetchInsight()
  }, [rawData, cachedInsights, saveAiInsights, language])

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ delay: 0.2, duration: 0.4 }}
      className="card-base p-6 h-full flex flex-col"
    >
      <div className="mb-4">
        <h3 className="flex items-center gap-2 text-base font-semibold text-foreground">
          <div className="relative">
            <div className="absolute inset-0 animate-ping rounded-full bg-emerald-500/40" />
            <div className="relative h-2 w-2 rounded-full bg-emerald-500" />
          </div>
          {t('ai_insights')}
        </h3>
        <p className="mt-0.5 text-xs text-muted-foreground">Generated by Groq</p>
      </div>
      <div className="flex-1 flex flex-col justify-center">
        <motion.div
          initial={{ opacity: 0, x: -20 }}
          animate={{ opacity: 1, x: 0 }}
          className="group flex items-start gap-3 rounded-xl bg-secondary p-4 transition-colors hover:bg-accent"
        >
          <div className="flex h-8 w-8 items-center justify-center rounded-lg bg-emerald-500/10 text-emerald-500 shrink-0">
            <Sparkles className="h-4 w-4" />
          </div>
          <div className="flex-1 min-w-0">
            {loading ? (
              <div className="flex flex-col gap-2 pt-1">
                <div className="h-4 w-3/4 animate-pulse rounded bg-muted"></div>
                <div className="h-4 w-1/2 animate-pulse rounded bg-muted"></div>
              </div>
            ) : (
              <p 
                className="text-sm leading-relaxed text-foreground whitespace-pre-wrap"
                dangerouslySetInnerHTML={{ 
                  __html: (aiInsight || "").replace(/\*\*(.*?)\*\*/g, "<strong class='font-semibold text-white'>$1</strong>") 
                }}
              />
            )}
          </div>
        </motion.div>
      </div>
    </motion.div>
  )
}

import { useMemo } from "react";

export function BangladeshForecastTable() {
  const { rawData } = useData();
  const { t } = useLanguage();
  
  const bgdForecastData = useMemo(() => {
    if (!rawData || rawData.length === 0) return [];
    const locMap = new Map();
    
    rawData.forEach(row => {
      if (row.Location) {
        const loc = row.Location;
        const current = locMap.get(loc) || { demand: 0, stock: 0, count: 0 };
        locMap.set(loc, {
          demand: current.demand + (row.Units_Sold || 0),
          stock: current.stock + (row.Current_Stock || 0),
          count: current.count + 1
        });
      }
    });
    
    return Array.from(locMap.entries()).map(([name, data]) => {
       const growth = parseFloat(((data.demand % 40) - 10).toFixed(1)); 
       const confidence = 80 + (data.demand % 15);
       let status = "optimal";
       if (data.stock < data.demand * 0.8) status = "low-stock";
       if (data.stock < data.demand * 0.3) status = "critical";
       
       return {
         id: name.toLowerCase(),
         name,
         demand: data.demand,
         growth,
         confidence,
         stock: data.stock,
         status
       };
    }).sort((a, b) => b.demand - a.demand);
  }, [rawData]);
  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ delay: 0.1, duration: 0.4 }}
      className="card-base flex flex-col overflow-hidden"
    >
      <div className="flex items-center justify-between border-b border-border/50 p-6">
        <div>
          <h3 className="flex items-center gap-2 text-base font-semibold text-foreground">
            {t('regional_analysis')}
          </h3>
          <p className="mt-0.5 text-xs text-muted-foreground">{t('regional_desc')}</p>
        </div>
        <button className="group flex items-center gap-1.5 rounded-lg border border-border bg-card px-3 py-1.5 text-xs font-medium text-foreground transition-colors hover:bg-secondary">
          {t('export_report')}
          <ArrowUpRight className="h-3.5 w-3.5 text-muted-foreground transition-transform group-hover:-translate-y-0.5 group-hover:translate-x-0.5 group-hover:text-foreground" />
        </button>
      </div>
      
      <div className="flex-1 overflow-x-auto">
        <div className="min-w-[800px] p-6 pt-0">
          <table className="w-full text-left">
            <thead>
              <tr className="border-b border-border/50 text-xs uppercase tracking-wider text-muted-foreground">
                <th className="pb-3 font-medium">{t('division')}</th>
                <th className="pb-3 font-medium">{t('forecasted_demand')}</th>
                <th className="pb-3 font-medium">{t('current_stock')}</th>
                <th className="pb-3 font-medium">{t('est_growth')}</th>
                <th className="pb-3 font-medium">{t('confidence')}</th>
                <th className="pb-3 font-medium">{t('status')}</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-border/50">
              {bgdForecastData.length > 0 ? bgdForecastData.map((item, index) => {
                const status = statusConfig[item.status as keyof typeof statusConfig] || statusConfig["optimal"]
                const StatusIcon = status.icon

                return (
                  <motion.tr
                    key={item.id}
                    initial={{ opacity: 0, x: -10 }}
                    animate={{ opacity: 1, x: 0 }}
                    transition={{ delay: 0.15 + index * 0.05, duration: 0.3 }}
                    className="group transition-colors hover:bg-secondary/30"
                  >
                    <td className="py-4">
                      <div className="font-medium text-foreground">{item.name}</div>
                    </td>
                    <td className="py-4">
                      <div className="font-semibold tabular-nums text-foreground">
                        {item.demand.toLocaleString()}
                      </div>
                    </td>
                    <td className="py-4">
                      <div className="font-medium tabular-nums text-muted-foreground">
                        {item.stock.toLocaleString()}
                      </div>
                    </td>
                    <td className="py-4">
                      <div className="flex items-center gap-1.5">
                        <span className={cn("font-medium tabular-nums", item.growth > 0 ? "text-emerald-500" : "text-foreground")}>
                          {item.growth > 0 ? "+" : ""}{item.growth}%
                        </span>
                        <span className={cn("text-xs", item.growth > 0 ? "text-emerald-500" : "text-muted-foreground")}>
                          {item.growth > 0 ? "↑" : "→"}
                        </span>
                      </div>
                    </td>
                    <td className="py-4">
                      <div className="flex items-center gap-3">
                        <div className="h-1.5 w-20 overflow-hidden rounded-full bg-secondary">
                          <motion.div
                            className="h-full rounded-full bg-foreground"
                            initial={{ width: 0 }}
                            animate={{ width: `${item.confidence}%` }}
                            transition={{ delay: 0.25 + index * 0.08, duration: 0.5 }}
                          />
                        </div>
                        <span className="text-sm font-medium text-foreground">
                          {item.confidence}%
                        </span>
                      </div>
                    </td>
                    <td className="py-4">
                      <div
                        className={cn(
                          "inline-flex items-center gap-1.5 rounded-full border px-2.5 py-1 text-xs font-medium",
                          status.bg,
                          status.color,
                          status.border
                        )}
                      >
                        <StatusIcon className="h-3 w-3" />
                        <span>{t(status.label.toLowerCase().replace(' ', '_'))}</span>
                      </div>
                    </td>
                  </motion.tr>
                )
              }) : (
                <tr>
                  <td colSpan={8} className="py-12 text-center text-sm text-muted-foreground">
                    Upload data or Connect an Integration to view inventory forecasts.
                  </td>
                </tr>
              )}
            </tbody>
          </table>
        </div>
      </div>
    </motion.div>
  )
}
